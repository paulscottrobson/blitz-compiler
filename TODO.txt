Convert compiler to use new system.
		- branches => 2
		- transfer to API
		- fix moveforward to use API.
		- remove anything like config.inc remaining (in object/simple)

Tests port over:

Versioning:

Fixes:
	Fix David Murray's program.
	Exiting runtime (fix END and errors)
	2 error handlers (before composite compile will work)
	
