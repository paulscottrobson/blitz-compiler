Todo:
		- Exiting runtime (fix END and errors)
		- Starting sequential write of output file.
		- Writing new stub.
		- Writing runtime.		

Tests port over:

Versioning:

Fixes:
		- copy down object code rather than leave it after the compiler/library stuff.	
		  (still h as to be on page boundary)