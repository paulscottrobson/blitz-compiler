TODO:
	Get code running on runtime.
	Get the show-variables code working again for defaults.
	Goto and pass 1.5
	We've caught up.
	

	GOSUB and IF THEN <various>
	Full Variable names
	Arrays

NOTES:
	Lots crashes (assert failure) occasionally on polynomials ?  Edge case rounding error probably.
	Bin$/Hex$ tests
	Maybe optimise +1/-1 ?