TODO:
	Make chainload and add the tokenised code.
	See what it compiles.
	Breakpoint on PC > $8000 and trap errors/save with active code.
	Check errors output
	Check code saves okay 
	Append runtime / Incorporate compiler automagically.
	See if it compiles under windows
	Try to disable SDL in compiler to avoid blink.

	X16 tokens.
		Figure out what BSAVE, VLOAD, BLOAD and BVLOAD actually *do*.
		New X16 tokens R43 

TODO List (X16)
---------------
	r42 (to add)	BLOAD BVLOAD BSAVE VLOAD 
	r43 (to add) 	LINPUT LINPUT# BINPUT# RPT$
	r43 (rejected)	HELP POINTER STRPTR 
	
Specifics
---------
	Generalise MoveObjectForward (?)
	Long Integer e.g. 2196679407 should show an error, or work, it loads the wrong number.
	Allow . as a constant ?


Won't work
----------
	Dynamic FNs don't work ; they are defined at compile time. Why would anyone do such a bizarre thing ?
