TODO:
	Terms: Unary Functions / Generation (re-entrant !)
		- X command.
		- Fix up MID$ and NOT
		- Check all functions

	Get Variables back in limited mode.
	Assignment
	Goto and pass 1.5
	We've caught up.
	
NOTES:
	Lots crashes (assert failure) occasionally on polynomials ? (probably VAL issue)

TODO: 
	Hex$ tests
