TODO:
	GOSUB/RETURN 
	IF THEN <various>
	Do some more benchmarking.
	FOR/NEXT design/implementation
		Step and End don't, can be any value.
	Full Variable names
	Arrays

NOTES:
	Bin$/Hex$ tests
	Maybe optimise +1/-1 ?


FOR 	Reference. 				[2]
		Reference type 			[1]
		Step (+1 optimise) 		[6]
		Terminal Value.	 		[6]
		Page/Target/Y values 	[4]
		
PRINT
		Print through a vector:

		PRINT.CHR

		TAB(x)	x PRINT.POS
			Print spaces until >= x
		SPC(x)  x PRINT.SPC
			Print x spaces
		, 		  PRINT.TAB
			Print to next tab stop, which is 10s.
		;

		PRINT.S
		PRINT.N

		Evaluate:
			With number either with leading space or -ve Space afterwards
			String ; output string.
