TODO:
	Read/Write commands 120..127
	Check string low vs allocate high.
	Start writing a test script 
	Code to get TI$/set TI$ (can't set TI ?)
	DEF FN / FN code.
	OPEN/CLOSE
	INPUT/INPUT#
	
NOTES:
	Closing channel resets default if that being used.
	BANK sets the PEEK/POKE bank
	Bin$/Hex$ tests / PETCAT in Python replacement.
	Integer for loops (not technically allowed but maybe force-int opt) (probably not required now)
	Maybe optimise +1/-1 ? (no)

Token list (standard)		
=====================
DEF FN / FN any variable, goes in with variable names design

OPEN CLOSE (CMD fix)
INPUT 
INPUT# 


Token list (X16)
================
VPOKE VLOAD SCREEN PSET LINE FRAME RECT CHAR MOUSE COLOR TEST RESET CLS LOCATE
BLOAD BVLOAD BVERIFY BANK VPEEK MX MY MB JOY 
HEX$ BIN$ (untested)

