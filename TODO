TODO:
	Add f.cmp and s.cmp calls.
	Terms: Parenthesis
	Terms: Unary Functions / Generation
	Get Variables back in limited mode.
	
NOTES:
	Lots crashes occasionally on polynomials ? (probably VAL issue)

