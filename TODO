TODO:
	DEF FN / FN code.
	Compiler Error Handler
	Start writing a test script 
	OPEN/CLOSE
	INPUT/INPUT#
	Code to get TI$/set TI$ (can't set TI ?)
	
NOTES:
	Closing channel resets default if that being used.
	BANK sets the PEEK/POKE bank

Token list (standard)		
=====================
DEF FN / FN any variable, goes in with variable names design
OPEN CLOSE (CMD fix)
INPUT 
INPUT# 


Token list (X16)
================
VPOKE VLOAD SCREEN PSET LINE FRAME RECT CHAR MOUSE COLOR TEST RESET CLS LOCATE
BLOAD BVLOAD BVERIFY BANK VPEEK MX MY MB JOY 
HEX$ BIN$ (untested)

