TODO:
	Improve disassembler (incorporate variable information)
	Start writing a test script.
	Arrays
	Code to get TI$/set TI$ (can't set TI ?)
	(Reset of token list)

NOTES:
	As Required: Move some other identifiers into shift mode ? [ GETCHANNEL, SETCHANNEL, maybe some of the functions ?]
	Closing channel resets default if that being used.
	Bin$/Hex$ tests
	Integer for loops (not technically allowed but maybe force-int opt)
	Maybe optimise +1/-1 ?
	BANK sets the PEEK/POKE bank

Token list (standard)		
=====================

DIM goes in with variable design.
DEF FN / FN any variable, goes in with variable names design

OPEN CLOSE (CMD fix)
INPUT 
INPUT# 


Token list (X16)
================
VPOKE VLOAD SCREEN PSET LINE FRAME RECT CHAR MOUSE COLOR TEST RESET CLS LOCATE
BLOAD BVLOAD BVERIFY BANK VPEEK MX MY MB JOY 
HEX$ BIN$ (untested)

