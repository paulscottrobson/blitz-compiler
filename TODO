TODO:
	Get Variables back in limited mode.
	- write identify variable and patch it in (no arrays)
	- write getsetxy and check it works (no arrays)
	Assignment and main loop.
	Get the show-variables code working again for defaults.
	Goto and pass 1.5
	We've caught up.
	
NOTES:
	Lots crashes (assert failure) occasionally on polynomials ? (probably VAL issue)

TODO: 
	Bin$/Hex$ tests
