TODO:
	FloatCompare issue ? 
	PRINT (?)
	GOSUB/RETURN and IF THEN <various>
	FOR/NEXT design/implementation
		Step and End don't, can be any value.
	Full Variable names
	Arrays

NOTES:
	Lots crashes (assert failure) occasionally on polynomials ?  Edge case rounding error probably.
	Bin$/Hex$ tests
	Maybe optimise +1/-1 ?


FOR 	Reference. 				[2]
		Reference type 			[1]
		Step (+1 optimise) 		[6]
		Terminal Value.	 		[6]
		Page/Target/Y values 	[4]
		